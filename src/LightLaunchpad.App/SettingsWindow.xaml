<Window x:Class="LightLaunchpad.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LightLaunchpad Settings"
        Width="520"
        Height="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Launchpad folder" VerticalAlignment="Center" />
        <TextBox x:Name="FolderTextBox" Grid.Column="1" Margin="0,0,0,10" />

        <TextBlock Grid.Row="1" Text="Hotkey" VerticalAlignment="Center" />
        <TextBox x:Name="HotkeyTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" />

        <TextBlock Grid.Row="2" Text="Icon size" VerticalAlignment="Center" />
        <ComboBox x:Name="IconSizeComboBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,10">
            <ComboBoxItem Content="Small" />
            <ComboBoxItem Content="Medium" />
            <ComboBoxItem Content="Large" />
        </ComboBox>

        <TextBlock Grid.Row="3" Text="Open mode" VerticalAlignment="Center" />
        <ComboBox x:Name="DisplayModeComboBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,10">
            <ComboBoxItem Tag="Launchpad" Content="启动台" />
            <ComboBoxItem Tag="Spotlight" Content="聚焦" />
        </ComboBox>

        <CheckBox x:Name="StartWithWindowsCheckBox"
                  Grid.Row="4"
                  Grid.Column="1"
                  Content="Start with Windows"
                  Margin="0,0,0,10" />

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="86" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="Save" Width="86" IsDefault="True" Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>